---
plugins: {}
filters: {}
rules:
  # https://github.com/kn1cht/textlint-rule-preset-ja-engineering-paper
  preset-ja-engineering-paper:

    # https://github.com/kn1cht/textlint-rule-preset-ja-engineering-paper?tab=readme-ov-file#unify-kuten-and-touten
    # 句点、読点は、デフォルトでは「．」（全角ピリオド）と「，」（全角カンマ）です。
    # より一般的な表現にします。
    unify-kuten-and-touten:
      kuten: "。"
      touten: "、"

  # https://github.com/textlint-ja/textlint-rule-preset-ja-spacing
  preset-ja-spacing:

    # Prettierで、Markdownで、日本語に英単語が混在する場合、英単語の周辺にスペースが挿入されます。
    # Prettierの動作に合わせるために、textlintでも英単語の周辺にスペースを挿入します。
    jaLetterSpace: true
    enLetterSpace: true

  # https://github.com/textlint-ja/textlint-rule-preset-ja-technical-writing
  preset-ja-technical-writing:

    # https://github.com/textlint-rule/textlint-rule-no-exclamation-question-mark
    no-exclamation-question-mark:
      allowHalfWidthExclamation: false
      allowFullWidthExclamation: false
      allowHalfWidthQuestion: false

      # 意図に反して `Disallow to use "？".` と表示されることがあります。
      # 次の設定によって表示しないようにします。
      # 例えば、Markdownの見出しなどで「？」を加えることを許します。
      allowFullWidthQuestion: true

  # https://github.com/textlint-ja/textlint-rule-preset-JTF-style
  preset-jtf-style:

    # 特定のルールを使いたくない場合、無効にできます。
    # https://github.com/textlint-ja/textlint-rule-preset-JTF-style?tab=readme-ov-file#faq:~:text=Q.-,%E7%89%B9%E5%AE%9A%E3%81%AE%E3%83%AB%E3%83%BC%E3%83%AB%E3%82%92%E4%BD%BF%E3%81%84%E3%81%9F%E3%81%8F%E3%81%AA%E3%81%84,-A.%20.textlintrc

    # 「かっこの外側、内側ともにスペースを入れません。」のエラーを出力しないようにします。
    # これにより、MarkdownでTODOアイテムなどで `[x] item` といった表記を許容します。
    # 2025-07時点で動作確認できていません。
    "3.3.かっこ類と隣接する文字の間のスペースの有無": false

    # 「コロン(：)を使用する場合は「全角」で表記します。」のエラーを出力しないようにします。
    # これにより、MarkdownでYAMLのような記法 `key: value` といった表記を許容します。
    "4.2.7.コロン(：)": false
